<section id="features" class="py-5 py-lg-6 bg-white">
    <div class="container">
        <div class="text-center mb-5">
            <h2 class="display-5 fw-bold text-dark mb-3">Fonctionnalités principales</h2>
            <p class="lead text-muted mx-auto" style="max-width: 700px;">
                Découvrez toutes les fonctionnalités conçues pour simplifier la gestion de votre club de handball
            </p>
        </div>

        <div class="row g-4">
            {% for feature in features %}
            <div class="col-md-6 col-lg-4">
                <div class="card border-0 shadow-sm h-100 hover-shadow transition-all">
                    <div class="card-body p-4">
                        <div class="d-flex align-items-start mb-3">
                            <div class="bg-{{ feature.color }} bg-opacity-10 rounded-3 p-3 me-3">
                                <i class="bi {{ feature.icon }} fs-3 text-{{ feature.color }}"></i>
                            </div>
                            <div>
                                <h3 class="h5 fw-bold text-dark mb-2">{{ feature.title }}</h3>
                                <p class="text-muted mb-0">{{ feature.description }}</p>
                            </div>
                        </div>

                        <a href="#" class="text-{{ feature.color }} fw-semibold text-decoration-none d-inline-flex align-items-center">
                            En savoir plus
                            <i class="bi bi-arrow-right-short ms-1"></i>
                        </a>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>

        {# Call to Action #}
        <div class="text-center mt-6 pt-5">
            <div class="card border-0 bg-primary bg-opacity-5 py-5">
                <div class="card-body">
                    <h3 class="h2 fw-bold text-dark mb-3">Prêt à transformer la gestion de votre club ?</h3>
                    <p class="lead text-muted mb-4">Rejoignez des centaines de clubs qui utilisent déjà HandStar Connect</p>
                    <div class="d-flex flex-wrap justify-content-center gap-3">
                        <a href="/register" class="btn btn-primary btn-lg px-5 py-3 fw-semibold">
                            Essayer gratuitement
                        </a>
                        <a href="#contact" class="btn btn-outline-primary btn-lg px-5 py-3 fw-semibold">
                            Demander une démo
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
